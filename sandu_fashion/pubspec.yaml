name: sandu_fashion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  curved_navigation_bar: ^1.0.6
  cloud_firestore: 5.5.0
  firebase_auth: ^5.3.3
  firebase_core: ^3.8.0   # Extra space before the caret

  random_string: ^2.3.1

  shared_preferences: ^2.3.3 
  image_picker : ^1.1.2
  firebase_storage: ^12.3.6
  cached_network_image:  
  flutter_stripe: 
  provider: ^6.0.5


  cupertino_icons: ^1.0.8
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/